<% if blood_group = BloodGroup.find(params[:id]) %>
  <form class="form-horizontal">
    <fieldset>
      <legend>Kan Grup</legend>
      <div class="control-group">
        <label class="control-label" for="disabledInput">Ad (*)</label>
        <div class="controls">
          <input type="text" class="input-xlarge disabled" id="disabledInput" value="<%= blood_group[:name] %>" disabled>
        </div>
      </div>
    </fieldset>

    <div class="form-actions">
      <a href="/<%= request[:controller] %>/<%= blood_group[:id] %>/edit" class="btn btn-primary">Düzenle</a>
      <a href="/<%= request[:controller] %>/<%= blood_group[:id] %>/destroy" class="btn btn-danger"
      onclick="return confirm('Bu kaydı gerçekten silmek istiyor musunuz?')">Sil</a>
    </div>
  </form>
<% else %>
  <center>
    Böyle Bir Grup Bulunamadı
  </center>
<% end %>
